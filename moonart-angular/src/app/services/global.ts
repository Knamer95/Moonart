export const global = {
    apiURL: 'http://localhost/moonart/api-rest-symfony/public' // API REST path (you can map it with virtual hosts and stuff)
    // apiURL: 'http://192.168.0.18/moonart/api-rest-symfony/public' // If you want to use ng serve --host xxx, put your host here (it may not be needed)
}
